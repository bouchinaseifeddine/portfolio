import 'package:flutter/material.dart';

class ContactTap extends StatelessWidget {
  const ContactTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'center 4',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
